#include<iostream>
#include<vector>
using namespace std;

int main(){
    vector<int> v1;
    int sze;
    cin>>sze;

    for (int i=0; i<sze; i++){
        cin>>v1[i];
    }

    sort(v.begin(), v.end()); //sort the array

    //remove the duplicates
    vector<int> v2;


    return 0;
}